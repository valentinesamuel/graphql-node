type Query {
  products: [Product]
  productsByPrice(min: Float!, max: Float!): [Product]
}

type Product {
  id: ID!
  description: String!
  price: Float!
  reviews: [Review]
}

type Review {
  rating: Int!
  comment: String
}